"""
URL configuration for the accounts app.

While API routes are registered via the project's central router, this
module can be used to expose additional account‑related routes in the
future. Currently it remains empty.
"""

from django.urls import path

app_name = 'accounts'

urlpatterns: list = [
    # Future account‑specific HTML views can be added here
]